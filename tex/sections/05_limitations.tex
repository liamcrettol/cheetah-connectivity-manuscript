\section{Limitations}
\label{sec:limitations}

% Write this section from the Cautions sheet directly. Each Critical-severity
% caution should be traceable to a sentence here. A reviewer who finds a
% limitation you did not name will distrust everything else.

\paragraph{Fixed historical cores.} \TODO{CAU-002. The single most important
% Sentence starter: "Because the cores were defined from historical data, ..."
limitation. Cores derive from 2010--2016 evidence and do not move; all temporal
signal is landscape-side.}

\paragraph{Structural, not functional.} \TODO{CAU-001.}
% Sentence starter: "The modeled paths represent structural permeability, so ..."

\paragraph{Sampling bias in the occurrence baseline.} \TODO{CAU-003. Absence of
% Sentence starter: "The density baseline may underrepresent ... because ..."
records is not evidence of absence.}

\paragraph{Resistance is expert-assigned.} \TODO{CAU-005. What the scenario ensemble
% Sentence starter: "Although we varied the resistance assumptions, ..."
does and does not compensate for.}

\paragraph{Static covariates.} \TODO{Roads, livestock, fences and terrain contribute
% Sentence starter: "Holding these variables static means that ..."
no temporal signal; consequences for interpreting change.}

\paragraph{Mapping completeness varies by country.} \TODO{CAU-010.}
% Sentence starter: "Differences in data coverage among countries may ..."

\paragraph{Geographic transferability.} \TODO{CAU-015.}
% Sentence starter: "The results should not be transferred to other regions without ..."

\paragraph{Scope.} \TODO{CAU-018. Climate, conflict and occupancy are deliberately out.}

\paragraph{Rainfall is not in the model.} \TODO{CHIRPS was never acquired. Vegetation
carries most of the temporal signal and the vegetation term is effectively bare-cover
fraction, which in a semi-arid system tracks rainfall. So some of what is reported as
landscape change may be interannual rainfall. This is a real limitation, not a
footnote.}

\paragraph{The vegetation term is narrower than it sounds.} \TODO{The transform reduces
to 1 + 0.09 x bare whenever the three VCF fractions sum to 100. Swapping tree for
non-tree cover at fixed bare cover changes nothing. Describe it as a bare-cover proxy,
not a model of vegetation structure.}

\paragraph{Only one core definition, one resolution.} \TODO{The planned three core
definitions and the 2 km and 5 km resolution checks were never run, so the claim in
\S\ref{sec:scale} that conclusions are not resolution artefacts is currently
unsupported. Either run them or move that claim here.}

\paragraph{Current is reported outside the cores.} \TODO{In a pairwise formulation the
cores inject and drain the current, so they carry the top decile by construction.
Masking them cleans up what is reported but does not remove focal-region influence from
the underlying solve. Also note the resistance graph has 34 connected components.}

\paragraph{Route location is less certain than route cost.} \TODO{49 of 180 route-years
fell below 80 percent overlap at 1 km, including 21 of the 32 priority links. A link can
be confidently cheap and still not have a confidently located route.}

\paragraph{Input series are not perfectly comparable.} \TODO{VIIRS switches from V21 to
V22 at the 2024 snapshot, breaking the pin-one-version rule. GHSL 2010 stands in for
2012, exactly at the two-year tolerance. Lights carry the smallest weight, which limits
the damage, but say it.}
% Sentence starter: "This study did not include ... and therefore cannot ..."
