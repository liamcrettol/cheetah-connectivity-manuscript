\section{Discussion}
\label{sec:discussion}

\subsection*{What the results mean, at the level the evidence supports}
% Sentence starters: "The main result of this study is ..." / "This result should be interpreted as ..." / "It does not demonstrate ..."
\TODO{First paragraph restates the headline finding in one sentence and immediately
bounds it. Resist writing "cheetah movement" anywhere in this section.}

\subsection{Where permeability has declined, and what co-occurred}
% Sentence starters: "Declines in modelled permeability were concentrated ..." / "These areas also showed ..." / "Because this is a spatial analysis, we interpret this as ..."
\TODO{H1. Spatial co-occurrence, never causation (CAU-014).
TRAP: the heading says "declined". Permeability did not broadly decline; resistance
fell slightly on balance and the direction is mixed. Retitle and reframe, or the
section argues against its own numbers.
Per-country heterogeneity cannot be reported for connectivity, only for covariate
change, so either build the attribution or say why it is absent.}

\subsection{The protection gap as a governance question}
% Sentence starters: "The distribution of priority connectivity outside protected areas means ..." / "This places conservation effort on ..." / "It does not by itself show that ..."
\TODO{H2. Governance framing.
THE FINDING IS SHARPER THAN "unprotected": the most concentrated flow is inside parks,
and the moderately-high connecting landscape around them is the least protected part
of the model domain, bottoming out near the 80th percentile of current.
SAY: connectivity for this species depends on land under other management. That is a
statement about where effort has to operate, not evidence that protected areas failed.
Keep the confound that parks are often sited on land of low agricultural value.
TRAP: do not write "connectivity is unprotected". The tail is well protected.}

\subsection{Robust versus model-sensitive linkages}
% Sentence starters: "Links that remained important across scenarios ..." / "Model-sensitive links are useful because ..." / "The main uncertainty comes from ..."
\TODO{H3. This is the paper's most transferable contribution. Robust locations
support action; model-sensitive locations define the survey agenda. Note the caveat
that scenarios sharing input layers can agree artificially.}

\subsection{From uncertainty to a survey design}
% Sentence starters: "The monitoring index is intended to help partners ..." / "A practical survey would begin by ..." / "The ranking should be updated when ..."
\TODO{H4. There is no monitoring index (CAU-020). Write this about the link-level
robust / uncertain split instead.
SAY: what a partner organisation would actually do with 13 uncertainty links, and that
accessibility and cost will reorder any list in practice.
STRONGEST POINT AVAILABLE: pair 17-24 is simultaneously the only bridge in the k=3
graph and the least spatially stable link in the set, zero route overlap in 2020. That
is the clearest example of a place where a survey would change the answer.}

\subsection{Comparison with regional connectivity work}
% Sentence starters: "Our approach is similar to previous regional studies in that ..." / "It differs because ..." / "The resistance values cannot be transferred directly because ..."
\TODO{Position against the elephant and multispecies transfrontier assessments
\parencite{naidoo2024kaza,brennan2020multispecies} as methodological neighbours, and
against the fencing-effects literature \parencite{osipova2018fencing}. Be explicit
that those are different species with different movement ecology: the methods
transfer, the resistance values do not (CAU-015).}
