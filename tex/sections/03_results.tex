\section{Results}
\label{sec:results}

% Results report numbers. No mechanism, no conservation implication, no
% "suggesting that". Every one of those belongs in the Discussion.

\subsection{Connectivity change, \studyperiod}
% Sentence starters: "Across the study period, modelled connectivity ..." / "The largest changes occurred ..." / "At the core-pair level, ..."
\TODO{H1. Magnitude and geography of change in current flow and effective distance.
Report per country and per core-pair. Figure~\ref{fig:change}.}

\subsection{Robust priority links}
% Sentence starters: "I identified [number] robust priority links ..." / "These links were concentrated ..." / "Their agreement across scenarios was ..."
\TODO{H3. How many links met the robustness criterion, and how concentrated they
are. Figure~\ref{fig:prioritylinks}, Table~\ref{tab:top20}.}

\subsection{Protected-area coverage of priority connectivity}
% Sentence starters: "Of the priority connectivity area, [value] was located ..." / "Compared with the predeclared null, this represented ..." / "The permutation test returned ..."
\TODO{H2. Observed share outside protected areas versus the predeclared null, with
the permutation result. Table~\ref{tab:pa}.}

\subsection{Model sensitivity}
% Sentence starters: "Results were consistent across ..." / "The links most sensitive to assumptions were ..." / "This indicates that ..."
\TODO{Agreement across the sensitivity grid. This subsection should be substantial.
If corridor rankings are unstable, say so here plainly; the Discussion then pivots
to the uncertainty-guided monitoring framing rather than a corridor map.
Figure~\ref{fig:agreement}.}

\subsection{Independent plausibility assessment}
% Sentence starters: "The plausibility assessment showed ..." / "Agreement was strongest for ..." / "The assessment was limited by ..."
\TODO{Outcome of \S\ref{sec:validation}. Report it even if weak or mixed.}

\subsection{Monitoring priorities}
% Sentence starters: "The highest-ranked monitoring cells were ..." / "Rank stability across weight sets was ..." / "These locations were prioritized because ..."
\TODO{H4. Ranked cells and their stability. Figure~\ref{fig:priority},
Table~\ref{tab:priority}.}

%------------------------------------------------------------------ float stubs
\begin{figure}[htbp]\centering
  \includegraphics[width=\textwidth]{Figure_2_temporal_current_evidence.png}
  \caption{Temporal current-flow evidence across the four environmental snapshots.
  High-current classes are descriptive structural-flow evidence, not validated corridors.}
  \label{fig:change}
\end{figure}

\begin{figure}[htbp]\centering
  \includegraphics[width=\textwidth]{Figure_3_robust_priority_links.png}
  \caption{Robust priority links retained across the sensitivity analyses.}\label{fig:prioritylinks}
\end{figure}

\begin{figure}[htbp]\centering
  \includegraphics[width=\textwidth]{Figure_1_high_current_concentration_2024.png}
  \caption{Ensemble agreement across the sensitivity grid.}\label{fig:agreement}
\end{figure}

\begin{figure}[htbp]\centering
  \includegraphics[width=\textwidth]{Figure_4_protected_area_context.png}
  \caption{Protected-area context for modeled priority connectivity.}\label{fig:priority}
\end{figure}

\begin{table}[htbp]\centering
  \caption{Covariates, native resolution, source years, and static/dynamic treatment.}
  \label{tab:covariates}
\end{table}

\begin{table}[htbp]\centering
  \caption{Top 20 robust priority links with country, protection status, and agreement score.}
  \label{tab:top20}
\end{table}

\begin{table}[htbp]\centering
  \caption{Protected-area coverage of high-current area against the predeclared null.}
  \label{tab:pa}
\end{table}

\begin{table}[htbp]\centering
  \caption{Monitoring priority ranking with component scores under alternative weightings.}
  \label{tab:priority}
\end{table}
