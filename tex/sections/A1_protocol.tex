\section{Analysis protocol as predeclared}
\label{app:protocol}
% Sentence starters: "Before examining connectivity results, we specified ..." / "The protocol was amended on [date] to add ..." / "The amendment did not change ..."
\TODO{Paste the locked protocol document here verbatim, with its version and date.
This is what lets you write "predeclared" in the Methods without overclaiming. It
should include: extent and exclusion rules, full CRS WKT, working resolution and the
alternatives to be tested, core definitions, the resistance scenario table, the
sensitivity grid, the robustness threshold $k$, the MCDA weight sets, and the claim
dictionary listing prohibited phrasings.}
