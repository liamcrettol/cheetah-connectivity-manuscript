\section{Analysis protocol as predeclared}
\label{app:protocol}
% Sentence starters: "Before examining connectivity results, we specified ..." / "The protocol was amended on [date] to add ..." / "The amendment did not change ..."
\TODO{Paste the protocol verbatim with its version and date, then a short list of
where the analysis ended up going differently.
Seven places to cover: core definition, the sensitivity grid, the robustness rule, the
scenario IDs, the network metrics, the protected-area design, and the plausibility
check. Plus the MCDA that was not built and the 2030 scenarios that came later.
This appendix is probably the most interesting thing you can hand a committee. It shows
a year of decisions and where the plan met reality, which is more than most capstones
can document.}
