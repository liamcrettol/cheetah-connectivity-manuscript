\section{Analysis protocol as predeclared}
\label{app:protocol}
% Sentence starters: "Before examining connectivity results, we specified ..." / "The protocol was amended on [date] to add ..." / "The amendment did not change ..."
\TODO{Paste the locked protocol verbatim, with version and date, and then an
amendment log immediately after it.
The amendment log is now the important half. The protocol and the analysis describe
different studies in at least seven places: core definition, the sensitivity grid, the
robustness rule, the scenario IDs, the network metrics, the protected-area design, and
the plausibility check. The MCDA was never built and the 2030 scenarios were added
after the lock.
Pasting the protocol without the log would make the divergences look hidden rather than
recorded. That is the difference between a predeclared study and one that reads as
though it were.}
